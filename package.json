{
  "name": "mediabag-client",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "next -p ${PORT:-3333}",
    "prestart": "npm run build",
    "start": "next start -p ${PORT:-3333}",
    "build": "next build",
    "preexport": "npm run build",
    "export": "next export",
    "preserve": "npm run export",
    "serve": "serve -p ${PORT:-3333} out"
  },
  "dependencies": {
    "isomorphic-unfetch": "^2.1.1",
    "next": "^6.1.1",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "serve": "^9.4.0"
  }
}
